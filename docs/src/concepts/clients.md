# Clients

A client is an application that provides an interface through which a user interacts with DLN.

DLN clients are built around [`dln-core`](/rustdocs/dln_core), which provides the shared client-side implementation of all DLN features crucially including DLN's communication and authentication model.
